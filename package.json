{
  "name": "scratchwork",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Scratchwork — the single-file Markdown renderer (template/) and the scratchwork dev CLI (cli/).",
  "scripts": {
    "build": "cd template && bun install && cd ../cli && bun run build",
    "build:template": "cd template && bun install && bun run build",
    "dev:template": "cd template && bun run dev",
    "test": "cd cli && bun test"
  },
  "dependencies": {
    "@effect/platform": "0.96.2",
    "effect": "3.21.4"
  }
}
