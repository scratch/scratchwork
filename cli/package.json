{
  "name": "scratchwork-cli",
  "version": "0.2.0",
  "type": "module",
  "private": true,
  "description": "scratchwork — Bun CLI built with Effect: dev server with hot reload, publishing, example files, and template export.",
  "bin": {
    "scratchwork": "./src/index.ts"
  },
  "scripts": {
    "build": "bun build.js",
    "ci": "bun run typecheck && bun run test && bun run build",
    "test": "bun test/run.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@effect/cli": "0.75.2",
    "@effect/platform": "0.96.2",
    "@effect/platform-bun": "0.90.0",
    "@effect/printer": "0.49.0",
    "@effect/printer-ansi": "0.49.0",
    "@effect/typeclass": "0.40.0",
    "@scratchwork/shared": "workspace:*",
    "effect": "3.21.4"
  },
  "files": [
    "example",
    "src"
  ],
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^6.0.3"
  }
}
