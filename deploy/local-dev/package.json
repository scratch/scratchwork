{
  "name": "@scratchwork/deploy-local-dev",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ci": "bun run typecheck && bun run test",
    "local": "bun local.ts",
    "test": "bun test --pass-with-no-tests",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@scratchwork/server-deploy-local": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^6.0.3"
  }
}
