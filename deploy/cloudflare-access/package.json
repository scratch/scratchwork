{
  "name": "@scratchwork/deploy-cloudflare-access",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "bun deploy.ts",
    "local": "bun local.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@scratchwork/server-deploy-cloudflare": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^6.0.3"
  }
}
