{
  "name": "@scratchwork/server-deploy-local",
  "version": "0.1.0",
  "type": "module",
  "description": "Local Bun deploy target for the Scratchwork server.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/scratch/scratchwork.git",
    "directory": "server/deploy-local"
  },
  "engines": {
    "bun": ">=1.2"
  },
  "files": [
    "dist"
  ],
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "ci": "bun run typecheck && bun run test",
    "test": "bun test",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@effect/platform": "0.96.2",
    "@effect/platform-bun": "0.90.0",
    "@scratchwork/server-core": "workspace:*",
    "effect": "3.21.4"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^6.0.3"
  }
}
