{
  "name": "@scratchwork/server-deploy-cloudflare",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Cloudflare Worker + R2 deployment package for the Scratchwork server.",
  "exports": {
    ".": "./src/index.ts",
    "./d1-db": "./src/d1-db.ts",
    "./r2-storage": "./src/r2-storage.ts",
    "./worker": "./src/worker.ts"
  },
  "scripts": {
    "check": "bun run typecheck && bun test",
    "dev": "bun scripts/dev.ts",
    "deploy": "bun scripts/deploy.ts",
    "smoke:local": "bun scripts/smoke-local.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@effect/platform": "0.96.2",
    "@scratchwork/server-core": "workspace:*",
    "effect": "3.21.4"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^6.0.3",
    "wrangler": "^4.107.0"
  }
}
