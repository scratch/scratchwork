{
  "name": "@scratchwork/e2e",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Full-loop publish e2e: a real server (local-dev, Cloudflare Worker under miniflare, AWS handler against LocalStack) driven by the real CLI through login → publish → serve → share → revoke, with a hermetic OAuth provider standing in for Google.",
  "scripts": {
    "ci": "bun run typecheck && bun run test",
    "conformance": "bun conformance/run.ts",
    "test": "bun test/run.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "3.1075.0",
    "@aws-sdk/client-s3": "3.1075.0",
    "@scratchwork/server-deploy-aws": "workspace:*",
    "@scratchwork/server-deploy-cloudflare": "workspace:*",
    "@scratchwork/server-deploy-local": "workspace:*",
    "miniflare": "4.20260701.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.152",
    "@types/bun": "^1.3.14",
    "playwright": "^1.61.1",
    "typescript": "^6.0.3"
  }
}
